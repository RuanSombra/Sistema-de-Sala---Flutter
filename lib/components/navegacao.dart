import 'package:flutter/material.dart';
import 'package:flutter_application_1/style/colors.dart';

/* ListTile NavegacaoDrawer(Icon icone, String title, BuildContext contexto, BuildContext tela){
  return ListTile(
    leading: icone,
    iconColor: preto,
    title: Text(title, style: TextStyle(fontFamily: 'Inter', fontSize: 13),),
    onTap: (){Navigator.push(contexto, MaterialPageRoute(builder: ))},
  );
} */