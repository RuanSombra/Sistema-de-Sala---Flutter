import 'package:flutter/material.dart';

class Acessibilidade extends StatelessWidget {
  const Acessibilidade({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
