import 'package:flutter/material.dart';

class Armazenamento extends StatelessWidget {
  const Armazenamento({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
